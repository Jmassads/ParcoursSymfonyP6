{% extends "base.html.twig" %}


{% block title %}{{ trick.title }}{% endblock %}

{% block body %}

    <div class="block">
        <h1>{{ trick.title }}</h1>
        <p>{{ trick.content }}</p>
    </div>

{% endblock %}